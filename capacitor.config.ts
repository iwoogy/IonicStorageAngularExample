import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_storage_angular_example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
